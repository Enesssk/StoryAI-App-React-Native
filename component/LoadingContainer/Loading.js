import React from 'react';
import { ActivityIndicator } from 'react-native';

const Loading = () => {
  return (
    <ActivityIndicator
      color={"white"}
      size="large" />
  )
}

export default Loading;